From 1cc870c43a37187d209c9622405b0a841fda3b8c Mon Sep 17 00:00:00 2001
From: Claude <noreply@anthropic.com>
Date: Thu, 20 Aug 2026 15:14:32 +0000
Subject: [PATCH] Fix .gitignore swallowing fixtures/payload.json

An unanchored 'payload.json' pattern matches at any depth, so the test
fixture was silently never committed and npm test died with ENOENT on a
fresh clone. Anchor the runtime-only ignores to the repo root.

Also harden the harness: fail with a readable message when a fixture is
missing, and restore mutated fixtures on every exit path so a crash
mid-run cannot leave a mutated file standing in as the known-good one.
---
 .gitignore                 |  4 +--
 fixtures/negative_tests.js | 22 +++++++++++++++
 fixtures/payload.json      | 58 ++++++++++++++++++++++++++++++++++++++
 3 files changed, 82 insertions(+), 2 deletions(-)
 create mode 100644 fixtures/payload.json

diff --git a/.gitignore b/.gitignore
index f7f5d69..8a92a4c 100644
--- a/.gitignore
+++ b/.gitignore
@@ -1,4 +1,4 @@
 .DS_Store
 node_modules/
-payload.json
-repo/
+/payload.json
+/repo/
diff --git a/fixtures/negative_tests.js b/fixtures/negative_tests.js
index 67e0650..4be6ce4 100644
--- a/fixtures/negative_tests.js
+++ b/fixtures/negative_tests.js
@@ -10,6 +10,15 @@ const PAY = 'fixtures/payload.json';
 const HTML = 'out/2026-08-19_morning_map.html';
 const PNG = 'out/2026-08-19_morning_map.png';
 
+const REQUIRED = [REC, PAY, HTML, PNG];
+const missing = REQUIRED.filter((f) => !fs.existsSync(f));
+if (missing.length) {
+  console.error(`Missing fixture(s): ${missing.join(', ')}`);
+  console.error('Run from the repo root: `npm test`. If a fixture is genuinely absent,');
+  console.error('rebuild it with `npm run fixture` (needs `npx playwright install chromium`).');
+  process.exit(2);
+}
+
 const goodRec = fs.readFileSync(REC, 'utf8');
 const goodPay = fs.readFileSync(PAY, 'utf8');
 const goodHtml = fs.readFileSync(HTML, 'utf8');
@@ -17,6 +26,19 @@ const goodPng = fs.readFileSync(PNG);
 
 const clone = () => JSON.parse(goodRec);
 
+/* The tests mutate tracked files in place. Restore them on any exit path,
+ * otherwise a crash mid-run leaves the working tree dirty and the next run
+ * reads a mutated "known-good" fixture. */
+function restore() {
+  fs.writeFileSync(REC, goodRec);
+  fs.writeFileSync(PAY, goodPay);
+  fs.writeFileSync(HTML, goodHtml);
+  fs.writeFileSync(PNG, goodPng);
+}
+process.on('exit', restore);
+process.on('SIGINT', () => process.exit(130));
+process.on('uncaughtException', (e) => { console.error(e); process.exit(1); });
+
 const CASES = [
   ['2  unclaimed numeral in tweet', 2, (r) => {
     r.tweet.text = r.tweet.text.replace('Core PCE', 'ETF flows 7.4t, Core PCE');
diff --git a/fixtures/payload.json b/fixtures/payload.json
new file mode 100644
index 0000000..2689d00
--- /dev/null
+++ b/fixtures/payload.json
@@ -0,0 +1,58 @@
+{
+  "slot": "morning_map",
+  "now_utc": "2026-08-19T23:30:00Z",
+  "instrument": "XAUUSD",
+  "market_state": {
+    "last": 3408.2,
+    "atr20": 28.4,
+    "asia_range": {
+      "high": 3414,
+      "low": 3399.5
+    },
+    "prior_day": {
+      "high": 3421.8,
+      "low": 3395.1,
+      "close": 3410
+    },
+    "levels": [
+      {
+        "label": "weekly open",
+        "price": 3402,
+        "source": "calc"
+      },
+      {
+        "label": "prior day high",
+        "price": 3421.8,
+        "source": "calc"
+      }
+    ]
+  },
+  "calendar": [
+    {
+      "event": "US Core PCE m/m",
+      "tier": 1,
+      "time_utc": "2026-08-20T12:30:00Z",
+      "consensus": 0.3,
+      "prior": 0.2
+    }
+  ],
+  "ere": null,
+  "prior_posts": [
+    {
+      "id": "p1",
+      "slot": "morning_map",
+      "generated_at": "2026-08-18T23:30:00Z",
+      "text": "Old map from yesterday about other levels entirely.",
+      "claims": [],
+      "card": {
+        "template": "levels",
+        "layout_variant": "ladder-left"
+      }
+    }
+  ],
+  "realized": null,
+  "session": {
+    "holiday": false
+  },
+  "session_today": null
+}
\ No newline at end of file
-- 
2.43.0

